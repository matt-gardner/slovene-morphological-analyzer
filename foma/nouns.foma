read lexc lexicon.lexc
define Lexicon

define FemNounStemming a "+Fem" -> 0 || _ "^";

define Cleanup "^" -> 0;

define Grammar Lexicon .o. FemNounStemming .o. Cleanup;
regex Grammar;
save stack slovene.bin
exit;
